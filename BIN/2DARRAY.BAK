  #include<stdio.h>
  #include<conio.h>
  void main (void)
  {
  clrscr();

  int r,c,h=0;
  int marks[100][100],roll[100][100];
  float sum,avg,per;
  char name[500],n;

	   printf("how many time for loop start ");
	   scanf("%d",&h);

  for (r=0;r<h;r++)
  {
  for(c=0;c<h;c++)
  {

  clrscr();
  printf("please enter your name : ");
  scanf("%s",&name[n]);

  printf(" please enter your marks: ");
  scanf("%d",&marks[r][c]);
  printf("please enter your ROLL: ");
  scanf("%d",&roll[r][c]);
  }
  }
	   clrscr();

  for (r=0;r<h;r++)
  {
  for(c=0;c<h;c++)
  {
  printf("NAME: %s\n",name[n]);
  printf("your marks total is:  %d\n",marks[r][c]);
  printf("your roll total is:  %d\n\n",roll[r][c]);
 // sum=sum+roll[r][c];


  }

  }
  /*
  avg=sum/4;
  per=sum*100/450;

  printf("\nsum is: %.2f\n",sum);
  printf("avg is :%.2f\n",avg);
  printf("per is : %.2f\n",per);
  if (per>80)
  {
  printf("%s your grade is 'A+'",name);
  }
  else if(per>70&&per<=80)
  {
  printf("%s your grade is 'A'",name);
  }
  else if(per>60&&per<=70)
  {
  printf("%s your grade is 'B'",name);
  }
  else if(per>50&&per<=60)
  {
  printf("%s your grade is 'C'",name);
  }
  else
  {
  printf("fail");
  }
     */



  getch();
  }








